import React from "react"

const MainPage = props => {
    return (
        <>
            <h1>PAGINA PRINCIPAL :)</h1>
            <p>User = {props.user}</p>
        </>
    )
}

export default MainPage